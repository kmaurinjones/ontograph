#!/usr/bin/env python3
"""
{{script_description}}
Outputs JSON for Claude to parse.
"""
import json
import sys
from pathlib import Path


def {{main_function_name}}({{function_args}}):
    """{{function_description}}"""
    # TODO: Implement function logic

    result = {
        "status": "success",
        # Add result fields here
    }

    return result


if __name__ == "__main__":
    {{#has_args}}
    if len(sys.argv) < {{min_args}}:
        print(json.dumps({"error": "{{usage_error}}"}))
        sys.exit(1)

    {{#arg_assignments}}
    {{arg_name}} = sys.argv[{{arg_index}}]
    {{/arg_assignments}}
    {{/has_args}}
    {{^has_args}}
    # No arguments required
    {{/has_args}}

    result = {{main_function_name}}({{call_args}})
    print(json.dumps(result, indent=2))
